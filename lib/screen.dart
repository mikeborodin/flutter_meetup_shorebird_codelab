import 'package:flutter/material.dart';

class SeparateFileScreen extends StatelessWidget {
  const SeparateFileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const ColoredBox(
      color: Colors.indigo,
    );
  }
}
